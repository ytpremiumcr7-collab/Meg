# Megalodon recovery manifest

This archive is a safe reconstruction after loss of a later generated ZIP.

Base of truth recovered from library:
`Megalodon_Topografia_v2_2026-08-13(1).zip`

Preserved as-is from that base:
- all original files in the base archive;
- `backend/tezcatlipoca/` was not modified by this recovery;
- existing `licitaciones_obra.py` was preserved as found in the recovered base; it was NOT replaced with the older fixture router from another baseline.

Added from the surviving PRR gate package:
- `reference/` INBAL control/negative fixtures and analysis artifacts.
- the surviving surgical implementation specification under `docs/recovery/`.

Important:
This is NOT claimed to be byte-identical to the lost generated ZIP. It is the safest complete reconstruction available from preserved artifacts. No existing application code was overwritten during recovery.
